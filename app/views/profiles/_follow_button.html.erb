<% if current_user.profile.following.include?(user.profile) %>
	<%= link_to 'Unfollow', unfollow_path(user), class: 'btn btn-danger', method: :delete, remote: true %>
<% else %>
	<%= link_to 'Follow', follow_path(id: user.id), class: 'btn btn-primary', remote: true %>
<% end %>