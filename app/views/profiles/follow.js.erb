$('#follow_<%= @follow.following_id %>').html("<%= j render partial: 'follow_button', locals: {user: @user} %>");

$("#follow_<%= @follow.following_id %> > .follow_button").mouseout(function(){
	$(this).hover( hoverIn,hoverOut );
});
